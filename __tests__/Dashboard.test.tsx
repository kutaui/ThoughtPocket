import { render } from '@testing-library/react';
import Dashboard from '@/app/(dash)/dashboard/page';

describe('Dashboard', () => {
  describe('Rendering', () => {
    it('should', () => {
      render(<Dashboard />);
    });
  });
  // describe('Behaviour', () => {});
});
